# Questions d'entretien

1. Pourquoi un appel direct au modèle depuis une route HTTP est-il rarement suffisant en production ?
2. Quelle est la différence entre un `Agent Runtime` et un `Application Service` ?
3. Pourquoi introduire un `Model Gateway` ?
4. Quels contrôles doit appliquer un `Tool Gateway` avant d’exécuter un outil sensible ?
5. Quelle différence fais-tu entre `conversation state`, `memory store` et `business database` ?
6. Quels signaux dois-tu tracer pour déboguer un système IA en production ?
7. Comment limiter le coût d’un agent autonome en production ?
8. Comment préparer un rollback sur un système IA dont le comportement dépend de prompts, schémas et modèles ?
9. Pourquoi les prompts et schémas doivent-ils être versionnés ?
10. Quels critères utiliseraient un AI Backend Engineer pour refuser une mise en production ?
